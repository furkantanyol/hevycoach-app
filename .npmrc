# Expo recommends the hoisted linker for pnpm: Metro resolves transitive deps it does not declare,
# which the isolated layout hides. Without this the release bundle step fails inside eas build --local.
node-linker=hoisted
